include ./button-mixin.jade

-
  var options = {
    classes: classes,
    id: id,
    attributes: attributes || {},
    disabled: disabled,
    tabindex: tabindex,
    iconLeft: iconLeft,
    text: text,
    iconRight: iconRight,
  };

+buttonMixin(options)
